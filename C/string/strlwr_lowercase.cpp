#include<stdio.h>
#include<string.h>

int main(){
	char str[20];
	printf("Enter a string:");
	gets(str);
	printf("Strings is %s",strupr(str));//strlwe,strupr,strrev all the function are used to same prosses 
	
	
	return 0;
}
